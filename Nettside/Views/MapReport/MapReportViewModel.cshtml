@using Nettside.ViewModels
@model Nettside.ViewModels.MapReportViewModel


<div class="container">
    <h2>Area Change Overview</h2>

    <div class="row">
        <div class="col-md-6">
            
                </tbody>
            </table>
        </div>

        <div class="col-md-6">
            <h3>Area Changes</h3>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Description</th>
                        <!-- Add other properties as needed -->
                    </tr>
                </thead>
                <tbody>
                    @foreach (var areaChange in Model.AreaChanges)
                    {
                        <tr>
                            <td>@areaChange?.Id</td>
                            <td>@areaChange?.Description</td>
                            <!-- Add other properties as needed -->
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>